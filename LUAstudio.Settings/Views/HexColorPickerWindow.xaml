<Window x:Class="LUAstudio.Settings.Views.HexColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Color Picker"
        Height="180"
        Width="260"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Hex Color"
                   Margin="0,0,0,6"/>

        <TextBox Grid.Row="1"
                 Text="{Binding SelectedHex, UpdateSourceTrigger=PropertyChanged}"
                 Height="28"/>

        <Border Grid.Row="2"
                Margin="0,12,0,0"
                Background="{Binding PreviewBrush}"
                BorderThickness="1"
                BorderBrush="Gray"
                CornerRadius="6"/>
        
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="0,10,0,0">

            <Button Width="70"
                    Margin="0,0,8,0"
                    Content="Cancel"
                    Click="Cancel_Click">
            </Button>

            <Button Width="70"
                    Content="OK"
                    Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>