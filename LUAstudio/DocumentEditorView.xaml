<UserControl x:Class="LUAstudio.DocumentEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:a="clr-namespace:ICSharpCode.AvalonEdit;assembly=ICSharpCode.AvalonEdit"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:LUAstudio.Converters"
             xmlns:ctrl="clr-namespace:LUAstudio.Controls"
             xmlns:local="clr-namespace:LUAstudio"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Background="{StaticResource BrushBgEditor}"
             AllowDrop="True"
             Drop="OnDrop">
    <UserControl.Resources>
        <conv:CountToVisibilityConverter x:Key="EmptyRootsVisible" Invert="True" />
        <conv:CountToVisibilityConverter x:Key="HasRootsVisible" />
        <conv:StringNullOrEmptyToVisibilityConverter x:Key="StringToVisibility" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibility" />
        <local:ExplorerNodeIconConverter x:Key="NodeIconConverter" />
        <Style TargetType="ScrollBar" BasedOn="{StaticResource ExplorerScrollBar}" />

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource BrushBgMenuPopup}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="0,1,0,0" />
            <Setter Property="Margin" Value="4,2" />
            <Setter Property="Height" Value="1" />
        </Style>
            
        <ContextMenu x:Key="EditorContextMenu" Opened="EditorContextMenu_Opened">
            <MenuItem Command="{x:Static ApplicationCommands.Cut}" />
            <MenuItem Command="{x:Static ApplicationCommands.Copy}" />
            <MenuItem Command="{x:Static ApplicationCommands.Paste}" />
            <Separator />
            <MenuItem Command="{x:Static ApplicationCommands.SelectAll}" />
            <Separator />
            <MenuItem Header="Go to Implementation"
                      Click="GoToSymbol_Click" />
            <MenuItem Header="Go to Declaration"
                      Click="GoToSymbol_Click" />
            <MenuItem Header="Toggle Breakpoint"
                      Click="ToggleBreakpoint_Click" />
            <MenuItem Header="Go to Line..."
                      Click="GoToLine_Click" />
            <MenuItem Header="Find"
                      InputGestureText="Ctrl+F"
                      Click="Find_Click" />
        </ContextMenu>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:CustomSearchPanel x:Name="SearchPanelControl" Grid.Row="0"/>

        
        <a:TextEditor x:Name="Editor"
                      Grid.Row="1"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="{StaticResource BrushBgEditor}"
                      BorderThickness="0"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      ShowLineNumbers="True"
                      WordWrap="{Binding WordWrap, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      ContextMenu="{StaticResource EditorContextMenu}"
                      Loaded="Editor_Loaded" />
    </Grid>
</UserControl>
